{
  "name": "marketplace",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "dev": "concurrently --names BACKEND,FRONTEND --prefix-colors yellow,cyan \"npm run dev --prefix backend\" \"npm run start --prefix frontend\"",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run start --prefix frontend",
    "build:client": "npm run client:build --prefix frontend",
    "start": "concurrently --names BACKEND,FRONTEND --prefix-colors yellow,cyan \"npm run start --prefix backend\" \"npm run start --prefix frontend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
